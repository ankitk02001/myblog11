package com.myblog.myblog11;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.function.DoubleToIntFunction;
import java.util.stream.Collectors;

import static java.util.Arrays.asList;

public class TestClass1 {
//   public static void main(String[] args) {
//        List<Employee> employees = Arrays.asList(
//                new Employee("mike",30,"chennai"),
//                new Employee("adam",25,"chennai"),
//                new Employee("stallin",31,"pune"),
//                new Employee("sam",34,"bangaluru")
//        );
//       Map<Integer, List<Employee>> collect = employees.stream()
//               .collect(Collectors.groupingBy(e->e.getAge()));
//       System.out.println(collect);
//
//       for (Map.Entry<Integer, List<Employee>> entry : collect.entrySet()) {
//           System.out.println("Age: " + entry.getKey());
//           for (Employee employee : entry.getValue()) {
//               System.out.println("  " + employee.getName());
//           }
//       }
//List<Employee> emps = employees.stream().filter(emp -> emp.getAge() > 30).collect(Collectors.toList());
//       // System.out.println(collect);
//
//        for(Employee e:emps){
//           // System.out.println(e.getName()+" "+e.getCity()+" "+e.getAge());
//            // stallin pune 31
//         //sam bangaluru 34
//        }
//        //to get name start with a
//        List<Employee> startWithA = employees.stream()
//                .filter(emp -> emp.getName().startsWith("a"))
//                .collect(Collectors.toList());
//        startWithA.forEach(employee -> System.out.println(employee));
//
//
//List<Integer> number= Arrays.asList(10,23,12,32,43,2,34,3,43,43,44);
// List<Integer> data = number.stream().filter(n1 -> n1%2==0).map(n2 -> n2 * n2).collect(Collectors.toList());
//  //data.forEach(num-> System.out.println(num));
//       System.out.println(data);


   }

